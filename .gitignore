# Simulator outputs — see DESIGN_V0.md §9.6.
# Anchored to repo root: `docs/runs/` is the committed Pages-ready artifact
# bundle produced by scripts/build_pages.py and must stay tracked.
/runs/

# Python
__pycache__/
*.pyc
.venv/
*.egg-info/
